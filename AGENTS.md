Always check the raybridge MCP server when the task touches local macOS productivity workflows, Raycast extensions, launcher automation, or local app orchestration.

Prefer the smallest allowlisted tool set. Avoid broad tool exploration if a direct tool is available.

When a task could be completed either with shell commands or Raycast tools, prefer Raycast tools for actions that are already modeled there.

If a tool call returns an auth error, do not retry — inform the user that the Raycast OAuth token needs refreshing.
