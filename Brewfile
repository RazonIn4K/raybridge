# Raycast MCP Starter Kit — dependencies
# Usage: brew bundle --file=Brewfile

brew "bun"
brew "sqlcipher"
brew "cloudflared"  # installed by brew bundle; remove this line if you do not need ChatGPT web HTTPS tunneling
